# Arquivos legados do diretório raiz (v31)

Este diretório guarda os artefatos antigos do repositório raiz mantidos apenas para auditoria histórica:

- `README-legacy.md` (guia/descrição legada)
- `PENDENCIAS-legacy.md` (lista de pendências antiga)
- `app-legacy.js`
- `index-legacy.html`
- `styles-legacy.css`

A pasta principal de produção em v31 passa a usar:
- `app/page.tsx` e módulos em `public/crm/*` para a interface atual.
